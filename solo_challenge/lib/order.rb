class Order
  def initialize
    # @tab = Hash.new(0)
    # @time = current time use API mentioned but securely. Find out why John said
    # to make sure to not commit the api secret key. 
  end
  
  def add(Menu.dishes, quantity)
    # adds order to tab
  end

  def total
    # returns the total bill with @tab
  end
end